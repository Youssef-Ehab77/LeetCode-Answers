/**
 * @param {string} a
 * @param {string} b
 * @return {string}
 */
var addBinary = function (a, b) {
    let carry = 0;
    let output = '';

    while (a !== '' || b !== '' || carry > 0) {

    }


};
addBinary('das', 'fds')